{% extends "email/base.html.twig" %}

{% block title %}Réinitialiser son mot de passe{% endblock %}

{% block body %}
    <section class="bg-white dark:bg-gray-900">
        <div class="py-8 px-4 mx-auto max-w-screen-xl lg:py-16 lg:px-6">
            <div class="max-w-screen-md">
                <p class="mx-auto mb-4 max-w-2xl font-light text-gray-500 md:mb-12 sm:text-xl dark:text-gray-400">Le lien pour réinitialiser le mot de passe :</p>
                <div class="mx-auto max-w-screen-sm text-sm text-left text-gray-500 newsletter-form-footer dark:text-gray-300">
                    <a href="{{ urlLink }}" class="font-medium text-primary-600 dark:text-primary-500 hover:underline">{{ urlLink }}</a>.
                </div>
            </div>
        </div>
    </section>

{% endblock %}